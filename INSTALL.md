# Setup
## Install npm
omit

## Install typescript and other required packages
### clone from this repository
```bash
git clone https://github.com/toshi-ara/sim-la-web.git
cd sim-la-web
npm install
```

### build
```bash
npm run build
```

### memo for this program
``` bash
cd (path to this repository)
npm install --save-dev typescript vite vitest vite-plugin-singlefile npm-check-updates
npx tsc --init
```
